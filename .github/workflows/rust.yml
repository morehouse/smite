name: Rust

on:
  push:
    branches: ["master"]
  pull_request:
    branches: ["master"]

env:
  CARGO_TERM_COLOR: always

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Rust Cache
        uses: Swatinem/rust-cache@v2

      - name: Install taplo
        run: curl -fsSL https://github.com/tamasfe/taplo/releases/latest/download/taplo-linux-x86_64.gz | gzip -d - | install -m 755 /dev/stdin /usr/local/bin/taplo

      - name: Check TOML formatting
        run: taplo format --check

      - name: Check Rust formatting
        run: cargo fmt --all --check

      - name: Run Clippy
        run: cargo clippy --all-targets --all-features -- -D warnings

      - name: Build encrypted-bytes scenario
        run: cargo build --verbose --features encrypted-bytes

      - name: Build encrypted-bytes scenario with nyx feature
        run: cargo build --verbose --features nyx,encrypted-bytes

      - name: Build noise scenario
        run: cargo build --verbose --features noise

      - name: Build noise scenario with nyx feature
        run: cargo build --verbose --features nyx,noise

      - name: Run tests
        run: cargo test --verbose
